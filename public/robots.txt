# Allow all crawlers
User-agent: *
Allow: /

# Sitemap
Sitemap: https://campusaxis.site/sitemap.xml

# Prevent crawling of admin pages
Disallow: /admin/
Disallow: /api/admin/

# Prevent crawling of user-specific pages
Disallow: /profile/
Disallow: /settings/

# Prevent crawling of search and filter pages
Disallow: /search/
Disallow: /*?*